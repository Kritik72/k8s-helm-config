apiVersion: v1
kind: Service
metadata:
  name: {{ include "aws-java-project.fullname" . }}
  labels:
    {{- include "aws-java-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aws-java-project.selectorLabels" . | nindent 4 }}
